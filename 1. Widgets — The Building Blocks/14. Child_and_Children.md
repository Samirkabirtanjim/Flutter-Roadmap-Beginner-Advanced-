/*
=====================================================
📌 CHILD vs CHILDREN IN FLUTTER 
=====================================================

In Flutter, everything is a Widget.
Widgets are arranged in a TREE structure.

To place widgets inside another widget, Flutter uses:
  1. child     → for ONE widget
  2. children  → for MULTIPLE widgets
*/


// --------------------------------------------------
// 🔹 child → SINGLE widget
// --------------------------------------------------

/*
Use `child` when a widget allows ONLY ONE widget inside it.
*/

Container(
  child: Text('Hello Flutter'),
);

/*
Common widgets that use `child`:
- Container
- Center
- Padding
- Align
- SizedBox
- Expanded
- GestureDetector
- SafeArea
- SingleChildScrollView
*/


// ❌ WRONG: Container does NOT support `children`
Container(
  // children: [               // ❌ ERROR
  //   Text('A'),
  //   Text('B'),
  // ],
);


// --------------------------------------------------
// 🔹 children → MULTIPLE widgets
// --------------------------------------------------

/*
Use `children` when a widget allows MORE THAN ONE widget.
`children` always takes a List<Widget>.
*/

Column(
  children: [
    Text('One'),
    Text('Two'),
    Text('Three'),
  ],
);

/*
Common widgets that use `children`:
- Row
- Column
- ListView
- GridView
- Stack
- Wrap
- Table
*/


// ❌ WRONG: Column does NOT support `child`
Column(
  // child: Text('Hello'),      // ❌ ERROR
);


// --------------------------------------------------
// 🧠 IMPORTANT CONCEPT
// --------------------------------------------------

/*
A widget that uses `child` can STILL contain many widgets
by placing a multi-child widget inside it.
*/

Container(
  child: Column(
    children: [
      Text('A'),
      Text('B'),
    ],
  ),
);

/*
Explanation:
- Container → has ONE child (Column)
- Column → has MANY children (Text widgets)
*/


// --------------------------------------------------
// 🌳 Widget Tree Example (Visual)
// --------------------------------------------------

Column( // children (many)
  children: [
    Container( // child (one)
      child: Text('Hi'),
    ),
    Center( // child (one)
      child: Icon(Icons.star),
    ),
  ],
);


// --------------------------------------------------
// 📊 QUICK COMPARISON
// --------------------------------------------------

/*
child:
- Accepts: ONE widget
- Type: Widget

children:
- Accepts: MULTIPLE widgets
- Type: List<Widget>
*/


// --------------------------------------------------
// 🚫 COMMON BEGINNER MISTAKES
// --------------------------------------------------

// ❌ Multiple widgets inside `child`
Container(
  // child: [
  //   Text('A'),
  //   Text('B'),
  // ],
);

// ❌ Forgetting List brackets in `children`
Column(
  // children:
  //   Text('A'),
  //   Text('B'),
);


// ✅ CORRECT
Column(
  children: [
    Text('A'),
    Text('B'),
  ],
);


// --------------------------------------------------
// ✅ FINAL RULE TO REMEMBER
// --------------------------------------------------

/*
ONE widget  → child
MANY widgets → children

Always check widget documentation to know
whether it uses `child` or `children`.
*/

